let coffeeOrder = [
    "Alex - Cortado",
    "Ben - Cortado",
     "Charlie - Whatever's new",
];
console.log(coffeeOrder);
//This is called an array, which is basically a list

let bestSongs = [
    "Magic Sword",
    "Peturbator",
    "Carpenter Brut",
];

console.log(bestSongs);

console.log(bestSongs[0])
//You can pull up a specific item out of aray using this

coffeeOrder[1] = "Ann - Vanilla latte";
console.log(coffeeOrder);
//You can change arrays using this

console.log(coffeeOrder.length);
coffeeOrder.push("Donna - espresso");
//You can add to the end of an array using .push
console.log(coffeeOrder);

coffeeOrder.pop();
console.log(coffeeOrder);
//You can remove the last item from the araay using .pop

//Create a list of 3 fave websites and add two once you've made the list, then remove the last website

//Research shift() unshift() slice() splice()

//https://github.com/Nathan-119/codenation.gitgit